export enum UploadImgType {
    Img = "img",
    HeadImg = "headImg",
    Comment = "comment",
    Plugins = "plugins",
    Notepad = "notepad"
}

export enum UploadFileType {
    Notepad = "notepad"
}
