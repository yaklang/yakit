import React from "react"
import {AIFileSystemProps} from "./AIFileSystemType"

const AIFileSystem: React.FC<AIFileSystemProps> = React.memo((props) => {
    return <div>系统</div>
})

export default AIFileSystem
