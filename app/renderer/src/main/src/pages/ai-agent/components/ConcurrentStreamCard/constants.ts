import type { CSSProperties } from 'react'

/** 子窗口卡片容器样式 */
export const CHILD_WINDOW_STYLE: CSSProperties = {
  height: '100%',
  backgroundColor: 'var(--Colors-Use-Basic-Background)',
}

/** 子窗口内容区样式 */
export const CHILD_CONTENT_WINDOW_STYLE: CSSProperties = {
  flex: 1,
}
