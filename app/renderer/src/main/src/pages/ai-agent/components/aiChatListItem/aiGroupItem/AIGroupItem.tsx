import React from 'react'
import { AIGroupItemProps } from './type'

const AIGroupItem: React.FC<AIGroupItemProps> = React.memo((props) => {
  return <div>AIGroupItem</div>
})

export default AIGroupItem
