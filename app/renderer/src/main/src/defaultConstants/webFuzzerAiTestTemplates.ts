export interface WebFuzzerAiTestTemplate {
  label: string
  prompt: string
}

/** Web Fuzzer AI 测试模板初始数据 */
export const DEFAULT_WEB_FUZZER_AI_TEST_TEMPLATES: WebFuzzerAiTestTemplate[] = [
  {
    label: 'SQL 注入深度探测',
    prompt:
      '作为高级安全专家，请深度分析当前 HTTP 请求的上下文。自动识别所有可能与后端数据库进行交互、拼接或查询的参数（包含 URL 传参、POST 表单、JSON 键值对以及特定的 HTTP 头部）。在这些潜在注入点上，自动构造最具代表性的 SQL 盲注（时间盲注/布尔盲注）、报错注入及联合查询 Payload。重点针对当前主流 WAF 的过滤规则进行字符级变异与绕过（如利用特定注释、大小写混淆、非标准空格、特殊编码），直接输出一组高价值的 Fuzz 探测请求列表。',
  },
  {
    label: '命令/代码注入探测',
    prompt:
      '仔细审查当前 HTTP 请求，识别可能被后端服务器用于系统调用、文件包含、反序列化或动态执行的敏感字段（如文件路径、模板渲染参数、命令字、图片处理参数等）。针对这些字段自动构造常见的系统命令拼接（如利用分号、管道符、换行符：`; id`, `&& whoami`, `|| id`, `\\n id`）以及自适应的命令执行回显/延时 Payload，同时生成用于探测远程代码执行（RCE）的特征用例。',
  },
  {
    label: 'XSS 跨站漏洞探测',
    prompt:
      '全面检查当前请求中所有由用户可控并可能在前端页面渲染输出的字段。针对这些输入点，自动构造包含各种特殊字符闭合（如单双引号、尖括号、反引号）的反斜杠转义测试，以及融合了现代浏览器特性的 HTML/JS 标签变形 Payload（如利用事件驱动 onload/onerror、SVG/IMG 标签属性混淆、避免触发常见关键词过滤的规避手段），用于精准测试前后端的过滤与转义边界。',
  },
  {
    label: '越权(IDOR)行为探测',
    prompt:
      '深度解析当前请求的业务语义，自动提取出所有疑似代表用户身份、订单编号、资产 ID、组织架构或租户标识的敏感字段（如 uid, id, order_number, account_id, uuid 等）。准确判断该请求的权限级别（读操作/写操作/删除操作），并自动生成一套针对这些标识符进行跨域、平行越权遍历、垂直越权尝试（如将普通 ID 变更为管理员特征 ID 或置空）的 Fuzz 测试方案，用以验证越权漏洞。',
  },
  {
    label: '隐藏权限与参数猜解',
    prompt:
      '基于当前请求的路由、方法（GET/POST/PUT/DELETE）以及当前展现的业务场景进行语义关联。根据安全审计经验，大胆推测该接口在后端可能隐式支持but未公开的管理参数、调试开关或权限覆盖字段（例如：`debug=1`、`admin=true`、`test=1`、`is_admin=1`、`version=v2`）。请将这些潜在的隐式参数以合理的格式（URL参数或JSON键值）自动融入并衍生出多组全新的 Fuzz 请求组合，用以测试鉴权绕过。',
  },
  {
    label: '业务边界值异常压测',
    prompt:
      '系统性分析当前请求的数据结构，识别其中所有的数字型、状态码、布尔值、时间戳、数组或枚举类型字段。抛弃传统的盲目字典爆破，自适应为这些字段生成一套极端的业务边界值测试矩阵：包括但不限于负数边界（-1）、零值（0）、大整数溢出（999999999）、空值（null/undefined）、非预期数据类型、特殊符号组合以及超长字符串，生成一个多参数组合的异常输入探测列表，用于压测后端业务逻辑的健壮性。',
  },
  {
    label: '认证凭证定向猜解',
    prompt:
      '专门针对涉及登录、密码重置、验证码校验、账户激活等身份认证相关的敏感请求。结合当前请求中展现的域名、上下文语义、甚至已有的用户名特征，基于黑客知识库与行业经验，生成一套最具针对性的高频弱口令、通用默认密码、特定行业特征的凭证组合 Fuzz 方案，避免盲目遍历，提升定向猜解的成功率。',
  },
  {
    label: 'WAF 自适应绕过',
    prompt:
      '聚焦于对抗高强度 WAF 拦截的场景。深入分析当前请求体内的核心 Payload 或敏感参数，针对可能触发防火墙规则阻断的特征字符（如 select, union, script, alert 等），不采用常规的单一编码，而是结合上下文嵌套、组合多种高级绕过编码（如：URL 双重编码、嵌套 Hex 编码、非标准 Base64 变异、Unicode 冗余混淆、多字节字符集绕过），直接生成一组用于规避特征检测的变异 Fuzz 序列。',
  },
  {
    label: '请求走私与架构混淆',
    prompt:
      '为了深度探测反向代理、前端 CDN（如 Nginx、Cloudflare）与后端 Web 服务器（如 Tomcat、Node.js、Weblogic）之间由于 HTTP 协议规范实现不一致导致的解析差异。请在严格保持原请求核心业务逻辑可执行的前提下，对该 HTTP 请求进行底层结构混淆重构。自动生成包含双写 `Content-Length`、`Transfer-Encoding: chunked` 分块传输注入、畸形 HTTP 头部空格混淆、或者将请求方法进行异常替换的 Fuzz 请求体，用以猜解和探测协议走私与架构层面的绕过漏洞。',
  },
]
